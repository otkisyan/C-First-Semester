#include <iostream>
#include <cmath>
#include <windows.h>
#include <stdlib.h>
#include <iomanip>
using namespace std;

// Найти наибольшее из двух действительных чисел, используя переключатель.
int main(){
 
SetConsoleCP(65001);
SetConsoleOutputCP(65001);

float a = 0, b = 0;
cin >> a >> b;


switch (a > b)
{
case 1:
    cout << "A > b" << endl;
    break;
    
default:
cout << "B > A" << endl;
    break;

    
}

 system("pause");
}
